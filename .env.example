DATABASE_URL='http://127.0.0.1:8080'
DATABASE_AUTH_TOKEN='local'

BETTER_AUTH_URL='http://localhost:3000'
BETTER_AUTH_SECRET=''
BETTER_TRUSTED_URL='http://localhost:3000'
AUTH_GOOGLE_ID=''
AUTH_GOOGLE_SECRET=''

BOOKMARK_API_URL='http://localhost:3000'

VITE_API_BASE_URL='http://localhost:3000'

# 32-byte hex (use: openssl rand -hex 32). Encrypts Discord webhook URLs at rest.
ENCRYPTION_MASTER_KEY=''

# --- AI recommendation (apps/worker-ai) ---
# The recommendation worker calls Cloudflare Workers AI directly via
# the env.AI binding, so it needs no GCP / Vertex AI / AI Gateway env.
# The only AI-related setting is the model name hard-coded in
# apps/worker-ai/src/index.ts.

# Used in the Discord embed link
WEB_BASE_URL='http://localhost:3000'